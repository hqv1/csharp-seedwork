namespace Hqv.Seedwork.Ordering
{
    /// <summary>
    /// Only requried because PropertyMapping takes a type and we're storing a collection of 
    /// different types of PropertyMapping. This interface lets use store a collection if IPropertyMapping.
    /// </summary>
    public interface IPropertyMapping
    {
        
    }
}